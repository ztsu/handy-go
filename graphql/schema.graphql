type User {
  id: String!
  email: String!
}

type Query {
  version: String!
}

input RegisterUser {
  email: String!
}

type Mutation {
  registerUser(user: RegisterUser!): User!
  math: MathMutation!
}

type MathMutation {
  inc(d: Int!): Int!
  dec(d: Int!): Int!
}